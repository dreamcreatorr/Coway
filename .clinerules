# Cline 项目规则（.clinerules）— Coway 项目
# 本文件会在每次会话开始时被 Cline 自动读取，作为必须遵守的规则。

## 沟通方式
- 始终使用中文回复。
- 回答和解释保持简洁明了，避免长篇大论。
- 回答聚焦问题本身，不扩展无关内容。

## 技术选择
- 技术方案优先采用 2026 年及以后的主流、现代、受支持的技术。
- 不采用已过时或已被官方停止维护的技术。
- 若项目已有既定技术栈，优先沿用现有约定。

## 执行方式（重要）
- 不主动执行命令行命令（包括 run_commands、构建、安装、测试等）。
- 任务需要的文件读取、修改，优先使用文件读取/编辑工具完成。
- 若某项操作确实必须运行命令才能完成，先向用户征求同意再执行。

## 工作原则
- 动手前先理解问题，遵循项目现有命名与代码风格。
- 信息不充分或需求模糊时，主动向用户提问确认，不做无依据假设。
- 修改后若可以，进行简单验证再交付。
